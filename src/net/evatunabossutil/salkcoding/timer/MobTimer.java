package net.evatunabossutil.salkcoding.timer;

public class MobTimer {
/*
    private static HashMap<Integer,MobTimerInfo> timerlist = new HashMap<>();

    public static Map<Integer,MobTimerInfo> getList(){
        return timerlist;
    }

    public static void addTimer(MobDamageRank info){
        if(timerlist.containsKey(info.getMobEntity().getEntityId()))
            return;
        MobTimerInfo timer = new MobTimerInfo(info);
        timer.setTask(Bukkit.getScheduler().runTaskTimer(Main.getInstance(),timer,0,20));
        timerlist.put(info.getMobEntity().getEntityId(),timer);
    }

    public static void removeTimer(MobDamageRank info){
        if(timerlist.containsKey(info.getMobEntity().getEntityId())) {
            MobTimerInfo timer = timerlist.get(info.getMobEntity().getEntityId());
            timer.mobDeath();
            timerlist.remove(info.getMobEntity().getEntityId());
        }
    }
*/
}
